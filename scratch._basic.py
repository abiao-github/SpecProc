import re

with open('src/core/basic_reduction.py', 'r') as f:
    lines = f.readlines()

# Since the file is 534 lines and modifications are extensive, I will create a python script that rewrites `basic_reduction.py` using AST or simple text replacements.

